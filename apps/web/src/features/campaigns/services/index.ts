/**
 * @file index.ts
 * @description Campaigns Service API Layer.
 */

import { Campaign } from '../types';

export async function fetchCampaigns(): Promise<Campaign[]> {
  // TODO: Call API endpoint GET /api/campaigns
  return [];
}
