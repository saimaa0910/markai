/**
 * @file index.ts
 * @description Notifications Services API Layer.
 */

import { NotificationItem } from '../types';

export async function fetchNotifications(): Promise<NotificationItem[]> {
  // TODO: Call backend API GET /api/notifications
  return [];
}
