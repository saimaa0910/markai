/**
 * @file index.ts
 * @description CRM API Services Layer.
 */

import { Contact, Deal } from '../types';

export async function fetchContacts(): Promise<Contact[]> {
  // TODO: Execute API call to GET /api/crm/contacts
  return [];
}

export async function fetchDeals(): Promise<Deal[]> {
  // TODO: Execute API call to GET /api/crm/deals
  return [];
}
