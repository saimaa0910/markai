/**
 * @file index.ts
 * @description SEO Services API Layer.
 */

import { KeywordMetric } from '../types';

export async function fetchKeywords(): Promise<KeywordMetric[]> {
  // TODO: Call backend API GET /api/seo/keywords
  return [];
}
